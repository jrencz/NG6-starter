import template from './hero.twig';
import controller from './hero.controller';
import './hero.scss';

const heroComponent = {
  bindings: {},
  template,
  controller
};

export default heroComponent;
