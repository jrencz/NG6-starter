import controller from './remote-configuration.controller';
import template from './remote-configuration.twig';

import './remote-configuration.scss';

const component = {
  bindings: {},
  template,
  controller,
};

export default component;
